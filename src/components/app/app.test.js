/*
import React from 'react';
import renderer from 'react-test-renderer';
import App from './app';
import {questions, settings} from "../../mocks/mocks";
*/

it(`App correctly renders welcome screen`, () => {
/*  const tree = renderer
    .create(<App gameTime={settings.gameTime}
      maxMistakes={settings.errorCount}
      mistakes={0}
      step={-1}
      onUserAnswer={jest.fn()}
      onWelcomeScreenClick={jest.fn()}
      questions={questions}/>).toJSON();
  expect(tree).toMatchSnapshot();*/
});

it(`App correctly renders genre question screen`, () => {
/*  const tree = renderer
    .create(<App gameTime={settings.gameTime}
      maxMistakes={settings.errorCount}
      mistakes={0}
      step={1}
      onUserAnswer={jest.fn()}
      onWelcomeScreenClick={jest.fn()}
      questions={questions}/>, {createNodeMock: () => {
      return {};
    }
    }).toJSON();
  expect(tree).toMatchSnapshot();*/
});

it(`App correctly renders artist question screen`, () => {
/*  const tree = renderer
    .create(<App gameTime={settings.gameTime}
      maxMistakes={settings.errorCount}
      mistakes={0}
      step={2}
      onUserAnswer={jest.fn()}
      onWelcomeScreenClick={jest.fn()}
      questions={questions}/>, {createNodeMock: () => {
      return {};
    }
    }).toJSON();
  expect(tree).toMatchSnapshot();*/
});

